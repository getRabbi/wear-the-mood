{
  "_": "COMMITTED PRODUCTION FEATURE POLICY. This file -- not env/prod.json, not a Codemagic env group, not a developer's laptop -- is the authority for which features a production artifact ships with. env/prod.json is generated by scripts/render_app_env.py, which merges these gates with the credentials from the environment; scripts/verify_local_cutout_release.py refuses to let a release build proceed when the generated file disagrees. Nothing here is a secret; every value is a literal 'true' or 'false' because Dart's bool.fromEnvironment reads only the exact string 'true'.",
  "profile": "prod",
  "gates": {
    "CUTOUT_EDITOR_ENABLED": "true",
    "LOCAL_CUTOUT_IMPROVE_ENABLED": "false",
    "LOCAL_BG_REMOVAL_ENABLED": "true",
    "LOCAL_BG_ANDROID_ENABLED": "true",
    "LOCAL_BG_IOS_ENABLED": "true",
    "LOCAL_BG_IOS_DIAGNOSTICS_ENABLED": "false"
  }
}
