fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
PyJWT[crypto]==2.13.0   # [crypto] pulls `cryptography` for ES256/RS256 (Supabase asymmetric JWTs)
sentry-sdk==2.61.1
asyncpg==0.31.0
httpx==0.28.1
python-multipart>=0.0.9   # Apache-2.0 — multipart/form-data parsing for the cutout-mask upload (§ BG upgrade Phase 7)
pillow>=10.0.0   # HPND — decode/compose cutout masks for the free cutout editor (api side); the worker also uses it for rembg
aioboto3>=13.0.0   # Apache-2.0 — async S3 client for Cloudflare R2 image storage (§2, §8)
openai>=1.50.0   # text-embedding-3-small — query embeddings for closet search (§2.1)
anthropic>=0.40.0   # MIT — Claude Sonnet AI stylist; runs synchronously in the api (§2.1)
feedparser>=6.0.0   # BSD-2 — RSS/Atom parsing for the news ingestion cron (§1 pillar 5)
firebase-admin>=6.5.0   # Apache-2.0 — FCM send for the daily-push cron (§20; PUSH_PROVIDER=fcm)
azure-storage-queue>=12.11.0   # MIT — Azure Storage Queue wake-signal bridge (§11.2); lazy-imported, unused when QUEUE_PROVIDER=stub
azure-identity>=1.19.0   # MIT — managed-identity auth for Azure Storage Queue (§11.13); lazy-imported
