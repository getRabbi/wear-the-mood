{
  "_": "PHYSICAL-DEVICE EVIDENCE LEDGER for local-first background removal (local BG §8, §12). Compilation is not validation and a mocked unit test is not a device, so a release may not ship native code that has never produced a real cutout on real hardware. Each entry is keyed by `native_fingerprint` -- a hash of that platform's native sources plus docs/bg/local_cutout_compatibility.json -- so ANY change to the engine, the plugin, the registration, the pinned ML Kit client, the Vision revision or the validated toolchain invalidates prior evidence and forces the matrix to run again. Append entries with `python3 scripts/verify_local_cutout_release.py --record-device-evidence ...` after a session; never hand-edit a fingerprint to make a build pass.",
  "android": [
    {
      "recorded": "2026-07-29",
      "commit": "a729bae",
      "app_version": "1.0.16+19",
      "native_fingerprint": "superseded-pre-selftest",
      "result": "pass",
      "runs": 11,
      "crashes": 0,
      "engine": "google_mlkit",
      "notes": "11 real-device Add Garment runs on the Play production build, 0 crashes, 2 runs at ~3990 ms which the owner accepted. Predates the selfTest contract and the health/telemetry work, so it no longer covers the shipping native code."
    }
  ],
  "ios": [],
  "pending": {
    "android": "REQUIRED before the next Play release. Current fingerprint ce512df42aa97fc6. The native sources changed (the selfTest contract and the urgent-preparation path), so the 11 passing runs from 1.0.16+19 no longer describe the shipping code. This is the guarantee working as designed, not an obstacle: the previous evidence was taken against an engine that has since been edited.",
    "ios": "REQUIRED before any ios-release. Current fingerprint 242d27c6cb8d354a. Apple Vision has NEVER produced a cutout on physical hardware, and the encoder defect fixed in bf945e2 means it could not have done so at any earlier point either -- encodeCutoutPNG returned nil on every device, every time, since it was written. LOCAL_BG_IOS_ENABLED is now true in the committed production policy, which is what makes the engine ship; the release verifier refuses an ios-release until a passing device run is recorded here, which is what stops it shipping unproven."
  }
}
